<div class="col-md-12">
  <div id="evento">
    <%= image_tag 'step-1.png', class: 'img-responsive img-center' %>
    <div class="center-block fondo-gris">
    <h1 class="mbottom">¿Por qué brindas?</h1>

    <div class="row">
      <div class="mitad text-right">
        <%= form_tag select_activity_path do %>
            <%= hidden_field_tag :email, @me['email'] %>
            <%= hidden_field_tag :name, @me['name'] %>
            <%= hidden_field_tag :post, "Brindo por mis grandes amigos" %>
            <p><%= submit_tag "Amistad", class: ' btn btn-lg same btn-black' %></p>
        <% end %>
      </div>
      <div class="mitad text-left">
        <%= form_tag select_activity_path do %>
            <%= hidden_field_tag :email, @me['email'] %>
            <%= hidden_field_tag :name, @me['name'] %>
            <%= hidden_field_tag :post, "Brindo por el éxito junto a " %>
            <p><%= submit_tag "Logro", class: 'btn btn-lg same btn-black' %></p>
        <% end %>
      </div>
    </div>


    <div class="row">
      <div class="mitad text-right">
        <%= form_tag select_activity_path do %>
            <%= hidden_field_tag :email, @me['email'] %>
            <%= hidden_field_tag :name, @me['name'] %>
            <%= hidden_field_tag :post, "Brindo por un año más junto a " %>
            <p><%= submit_tag "Cumpleaños", class: 'btn btn-lg same btn-black' %></p>
        <% end %>
      </div>
      <div class="mitad text-left">
        <%= form_tag select_activity_path do %>
            <%= hidden_field_tag :email, @me['email'] %>
            <%= hidden_field_tag :name, @me['name'] %>
            <%= hidden_field_tag :post, "Brindo por esta navidad junto a " %>
            <p><%= submit_tag "Navidad", class: 'btn btn-lg same btn-black' %></p>
        <% end %>
      </div>
    </div>



    </div>
  </div>
</div>
